"""FSM configuration system for loading, validating, and building FSM instances.

This package provides:
- **loader**: Load FSM configurations from files, dicts, and templates
- **schema**: Pydantic schemas defining FSM configuration structure
- **builder**: Build executable FSM instances from configurations
- **validator**: Validate FSM configurations against schemas

Configuration files can be JSON or YAML format and define FSM states,
transitions, resources, and execution parameters.
"""
